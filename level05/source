main:
	char str[100]
	int i = 0;
	int len;

	fgets(str, 100, stdin)
	while (1)
		len = 0;
		while (str[len] != 0)
			len++;
		if (i == len)
			printf(str)
			exit(0)
		if (str[i] > '@' && str[i] < '[')
			str[i] = str[i] ^ 0x20 # LOWERCASE
		i++;
